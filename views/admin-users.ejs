<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>👥 User List</h2>
  <a href="/admin/users/create" class="btn btn-success">➕ Create New User</a>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Username</th>
      <th>Display Name</th>
      <th>Auth Type</th>
      <th>Can Login Locally</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.displayName %></td>
        <td><%= user.authType %></td>
        <td><%= user.canLoginLocally ? 'Yes' : 'No' %></td>
        <td>
          <% if (user.authType === 'local') { %>
            <a href="/admin/users/edit/<%= user.id %>">Edit</a>
          <% } else { %>
            <span class="text-muted">SAML user – no edit</span>
          <% } %>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table>

