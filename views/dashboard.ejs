<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - SAML Training App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <a class="navbar-brand" href="#">SAML Training App</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a href="/logout" class="nav-link">Logout</a></li>
      </ul>
    </div>
  </nav>

  <main class="container mt-4">
    <h1>Welcome, <%= user.displayName || user.username %>!</h1>
    <p><strong>Username:</strong> <%= user.username %></p>
    <p><strong>Email:</strong> <%= user.email || 'N/A' %></p>
    <p><strong>First Name:</strong> <%= user.firstName || 'N/A' %></p>
    <p><strong>Last Name:</strong> <%= user.lastName || 'N/A' %></p>
    <p><strong>Manager:</strong> <%= user.manager || 'N/A' %></p>
    <p><strong>Title:</strong> <%= user.title || 'N/A' %></p>
    <p><strong>Authentication Type:</strong> <%= user.authType || 'local' %></p>

    <% if (user.authType === 'saml' && user.samlAssertion) { %>
      <hr />
      <h4>SAML Raw Assertion</h4>
      <pre style="background:#f8f9fa; padding:1em; max-height:300px; overflow:auto;"><%= user.samlAssertion %></pre>
    <% } %>

    <% if (user.isAdmin) { %>
      <hr />
      <a href="/admin" class="btn btn-warning">Admin Panel</a>
    <% } %>
  </main>
</body>
</html>

